{
  "name": "node-orb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hardened Node 24 + TypeScript template for Docker/OrbStack — see README.md",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "test": "node --test \"src/**/*.test.ts\"",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "24.13.2",
    "typescript": "6.0.3"
  }
}
